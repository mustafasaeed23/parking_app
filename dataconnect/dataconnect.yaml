specVersion: "v1beta"
serviceId: "parkingapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "parkingapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
